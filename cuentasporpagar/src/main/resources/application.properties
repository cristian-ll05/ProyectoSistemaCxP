server.port=8090
spring.application.name=cuentasporpagar
# Datos de la conexión a MySQL (XAMPP)
spring.datasource.url=jdbc:mysql://localhost:3306/sistema_cpp
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA / Hibernate
# 'update' hace que Spring cree las tablas por ti si no existen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# CONFIGURACIÓN DE SEGURIDAD (Login Maestro)
#spring.security.user.name=admin
#spring.security.user.password=12345